darkmode: true
footer:
  icon:
    name: fa fa-user
    color: "#808080"

creative_commons:
  license: by-nc-sa
  sidebar: true
  post: true
  language: zh-TW

scheme: Mist

menu:
  home: / || fa fa-home
  about: /about/ || fa fa-user
  tags: /tags/ || fa fa-tags
  categories: /categories/ || fa fa-th
  # archives: /archives/ || fa fa-archive
  privacy: /about/privacy-policy || fas fa-user-shield

sidebar:
  display: hide

social:
  GitHub: https://github.com/j4nusl1n || fab fa-github
  Resume: /about/resume || fa fa-file-text

local_search:
  enable: true
  trigger: manual

google_analytics:
  tracking_id: G-648VTZD170

custom_file_path:
  head: source/_data/head.njk
  sidebar: source/_data/sidebar.njk

# Allow to cache content generation.
cache:
  enable: true

# Remove unnecessary files after hexo generate.
minify: false

# Open graph settings
# See: https://hexo.io/docs/helpers#open-graph
open_graph:
  enable: true
  options:
    type: blog

# Show multilingual switcher in footer.
language_switcher: false

# ---------------------------------------------------------------
# CDN Settings
# See: https://theme-next.js.org/docs/advanced-settings/vendors
# ---------------------------------------------------------------

vendors:
  # The CDN provider of NexT internal scripts.
  # Available values: local | jsdelivr | unpkg | cdnjs | custom
  # Warning: If you are using the latest master branch of NexT, please set `internal: local`
  internal: cdnjs
  # The default CDN provider of third-party plugins.
  # Available values: local | jsdelivr | unpkg | cdnjs | custom
  # Dependencies for `plugins: local`: https://github.com/next-theme/plugins
  plugins: cdnjs
  # Custom CDN URL
  # For example:
  # custom_cdn_url: https://cdn.jsdelivr.net/npm/${npm_name}@${version}/${minified}
  # custom_cdn_url: https://cdnjs.cloudflare.com/ajax/libs/${cdnjs_name}/${version}/${cdnjs_file}
  custom_cdn_url:

# Assets
# Accelerate delivery of static files using a CDN
# The js option is only valid when vendors.internal is local.
css: css
js: js
images: images

# Google Webmaster tools verification.
# See: https://developers.google.com/search
google_site_verification: tJfngue2homYInPbMEX8esxdAl2ibFeV5_KxILnJjbk

# Post wordcount display settings
# Dependencies: https://github.com/next-theme/hexo-word-counter
symbols_count_time:
  separated_meta: true
  item_text_total: false

favicon:
  small: /images/favicon-16x16.png
  medium: /images/favicon-32x32.png
  apple_touch_icon: /images/apple-touch-icon.png
  safari_pinned_tab:
  android_manifest: /site.webmanifest

recent_post:
  enable: true
  title: 近期文章
  layout: block
